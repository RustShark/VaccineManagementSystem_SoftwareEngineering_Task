package com.vaccinemanagementsystem.Main;

import static org.junit.Assert.*;
import org.junit.Test;

import java.time.LocalDate;
import java.util.Date;
import net.sourceforge.jdatepicker.impl.JDatePanelImpl;
import net.sourceforge.jdatepicker.impl.JDatePickerImpl;
import net.sourceforge.jdatepicker.impl.UtilDateModel;

public class calendarTest {

	/*
	@Test
	public void testGetAndSet() {
		LocalDate now = LocalDate.now();
		//UtilDateModel model = new UtilDateModel();
		
		model.setDate(now.getYear(), now.getMonthValue()-1, now.getDayOfMonth());
		
		assertEquals(model.getYear(), now.getYear());
		assertEquals(model.getMonth()+1, now.getMonthValue());
		assertEquals(model.getDay(), now.getDayOfMonth());
	}
	*/
}
